\begin{figure}[ht]
  \centering
  \begin{tikzpicture}[
      node distance=1.6cm and 1.2cm,
      every node/.style={font=\sffamily},
      box/.style={draw, minimum width=2.2cm, minimum height=1.2cm,
      align=center},
      imagebox/.style={draw, minimum width=2.2cm, minimum
      height=1.5cm, align=center, fill=gray!10},
      annot/.style={align=left}
    ]

    % Nodes
    \node[annot] (input) at (-4.2,0) {Input Image};
    \node[imagebox, right=of input] (patch)
    {\includegraphics[width=2.2cm,height=1.5cm]{Cap4/Figures/histopathology_patch.jpg}\\ \small
    Patch};
    \node[box, right=of patch] (model) {$f(X_i; \theta) =
    [\hat{\mathbf{y}} \mid \Lambda_R] $};
    \node[box, right=of model] (loss) {TCE$_{\text{SS}}$ Loss};

    % Arrows
    \draw[->] (input) -- (patch);
    \draw[->] (patch) -- (model);
    \draw[->] (model) -- (loss);

    % Annotator masks
    \node[annot, below=2cm of model] (annots) {
      Annotators Masks\\[1mm]
      $
      \begin{bmatrix}
        y_1 \\
        y_2 \\
        \vdots \\
        y_R
      \end{bmatrix}$
    };
    \draw[->] (annots.north) -- ($(model.south)$);

  \end{tikzpicture}
  \caption{Working mechanism of the proposed Truncated Generalized
    Cross Entropy for Semantic Segmentation (TGCE$_{SS}$) loss
    function. The loss combines reliability maps $\Lambda_r$ with the
    model's predictions $f(\mathbf{X};\theta)$ to compute a weighted
  loss that accounts for annotator reliability across different image regions.}
  \label{fig:loss_mechanism}
\end{figure}
